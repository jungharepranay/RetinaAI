You are a senior machine learning engineer improving an existing multi-label retinal disease detection system built on the ODIR-5K dataset.

The project already contains the following structure:

EDI_FINAL/
├── src/
│   ├── data_loader.py
│   ├── dataset_builder.py
│   ├── preprocessing.py
│   ├── model.py
│   ├── train.py
│   ├── evaluate.py
│   └── predict.py
├── dataset/odir/
│   ├── preprocessed_images/
│   └── full_df.csv
├── models/
├── reports/
└── app/

The current system already performs:

multi-label classification

DenseNet121 transfer learning

tf.data pipeline

data augmentation

class imbalance weighting

early stopping

evaluation metrics

However performance must be improved and the pipeline made more robust.

Your task is to improve the ML pipeline while keeping the same architecture and project structure.

Do NOT rewrite the project. Only add improvements.

1. Improve Image Preprocessing

Modify src/preprocessing.py.

Add green channel retinal enhancement with CLAHE.

Fundus images contain most diagnostic information in the green channel.

Pipeline:

RGB Image
↓
Extract Green Channel
↓
Apply CLAHE contrast enhancement
↓
Convert back to 3-channel image
↓
Resize to 224×224
↓
Normalize

Implement using OpenCV:

cv2.split()
cv2.createCLAHE()
cv2.merge()

This preprocessing should run inside the TensorFlow pipeline so it works during training and inference.

2. Improve Data Augmentation

Update dataset_builder.py.

Current augmentation includes:

RandomFlip
RandomRotation
RandomZoom
RandomContrast

Add additional augmentation:

RandomBrightness
RandomTranslation
RandomCrop

Ensure augmentation is applied only to training dataset.

Validation and test datasets must not include augmentation.

3. Implement Focal Loss for Imbalanced Data

Modify src/model.py.

Add an optional Focal Loss implementation.

Definition:

def focal_loss(gamma=2.0, alpha=0.25)

Use TensorFlow operations.

Allow the user to choose between:

BinaryCrossentropy
FocalLoss

through a parameter in build_model().

Default should remain BinaryCrossentropy to preserve compatibility.

4. Implement Per-Disease Threshold Optimization

Modify src/evaluate.py.

Currently predictions use a single threshold:

threshold = 0.3

Instead implement per-class thresholds.

Procedure:

Use ROC curves for each disease

Compute optimal threshold maximizing F1

Save thresholds to:

reports/optimal_thresholds.json

Use these thresholds during evaluation and prediction.

5. Improve Training Strategy

Modify src/train.py.

Update training configuration.

Current:

Phase 1: 15 epochs
Phase 2: 35 epochs

Keep this but add:

Mixed precision training when GPU is available

Add:

tf.keras.mixed_precision.set_global_policy('mixed_float16')

only when GPU is detected.

6. Improve Dataset Diagnostics

During training, print additional statistics:

dataset size
label distribution
class imbalance ratios

Add visualization function that plots disease frequency and saves to:

reports/label_distribution.png
7. Improve Evaluation Reporting

Enhance evaluate.py.

In addition to current metrics compute:

Precision Macro
Recall Macro
F1 Macro
Precision Micro
Recall Micro
F1 Micro
ROC-AUC Macro
ROC-AUC Micro
Hamming Loss
PR-AUC

Save results to:

reports/metrics_summary.json
reports/classification_report.txt
reports/confusion_matrices.npy
8. Add Grad-CAM Explainability

Create new file:

src/gradcam.py

Implement Grad-CAM visualization.

The script should:

Load trained model

Generate heatmap for prediction

Overlay heatmap on retinal image

Save visualization

Output path:

reports/gradcam_examples/

This allows visualization of which retinal regions caused disease predictions.

9. Improve Prediction Output

Modify src/predict.py.

Prediction should output:

Detected Diseases
Confidence score for each disease

Example output:

Detected diseases:
Myopia (0.82)
Cataract (0.64)

Use the optimized per-class thresholds.

10. Ensure Backward Compatibility

Do NOT change:

folder structure
FastAPI deployment
dataset format
existing model architecture

All improvements must integrate with the existing project.

11. Expected Outcome

After implementing these improvements the system should achieve approximately:

Macro F1: 0.45 – 0.55
Micro F1: 0.50 – 0.65
ROC-AUC: 0.80 – 0.88

on the ODIR-5K dataset.

12. Deliverables

Modify the following files:

src/preprocessing.py
src/dataset_builder.py
src/model.py
src/train.py
src/evaluate.py
src/predict.py

Add new module:

src/gradcam.py

Ensure the project still runs with:

python -m src.train
python -m src.evaluate
python -m src.predict path/to/image.jpg

and saves the trained model to:

models/retinal_model.keras